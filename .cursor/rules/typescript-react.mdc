---
description: TypeScript and React conventions for this codebase
globs: **/*.{ts,tsx}
alwaysApply: false
---

# TypeScript & React

- Use functional components and hooks
- Prefer explicit types at module boundaries; avoid `any`
- Colocate component-specific types with the component
- Run `npm run lint` after substantive TS/TSX edits
